from setuptools import setup, find_packages

setup(
    name="mental_health_chatbot",
    version="0.1.0",
    description="Fine-tuned empathetic chatbot (DistilGPT2 + EmpatheticDialogues)",
    packages=find_packages(),
    install_requires=[],
)
